#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gettext-extract/bin/gettext-extract" "$@"
  ret=$?
else 
  node  "$basedir/../gettext-extract/bin/gettext-extract" "$@"
  ret=$?
fi
exit $ret
